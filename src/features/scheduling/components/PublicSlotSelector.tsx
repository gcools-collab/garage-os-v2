import type{AvailabilitySlot}from"../types/scheduling"
export function PublicSlotSelector({slots}:{readonly slots:readonly AvailabilitySlot[]}){if(!slots.length)return <p className="rounded-lg border p-4 text-sm text-[var(--live-muted-foreground)]">Aucun créneau en ligne n’est actuellement disponible. Vous pouvez transmettre votre demande sans rendez-vous.</p>;const groups=Map.groupBy(slots,item=>item.dateLabel);return <fieldset className="border-0 p-0"><legend className="mb-4 text-lg font-semibold">Choisissez votre rendez-vous</legend><div className="space-y-5">{[...groups].map(([day,items])=><div key={day}><h3 className="mb-2 capitalize">{day}</h3><div className="flex flex-wrap gap-2">{items.map(item=><label key={item.startsAt} className="cursor-pointer"><input className="peer sr-only" type="radio" name="appointmentStartsAt" value={item.startsAt}/><span className="flex min-h-11 min-w-20 items-center justify-center rounded-lg border px-4 peer-checked:border-[var(--live-primary)] peer-checked:bg-[var(--live-primary)] peer-checked:text-[var(--live-primary-foreground)] peer-focus-visible:outline-2">{item.timeLabel}</span></label>)}</div></div>)}</div></fieldset>}
