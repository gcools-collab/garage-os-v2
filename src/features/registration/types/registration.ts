export const registrationProcedureTypes = ["CHANGE_OF_OWNER", "ADDRESS_CHANGE", "DUPLICATE", "REGISTRATION", "OTHER"] as const
export type RegistrationProcedureType = (typeof registrationProcedureTypes)[number]
export const registrationCaseStatuses = ["NEW", "WAITING_FOR_DOCUMENTS", "DOCUMENTS_RECEIVED", "UNDER_REVIEW", "INCOMPLETE", "READY", "IN_PROGRESS", "COMPLETED", "CANCELLED"] as const
export type RegistrationCaseStatus = (typeof registrationCaseStatuses)[number]
export const registrationDocumentStatuses = ["MISSING", "UPLOADED", "UNDER_REVIEW", "ACCEPTED", "REJECTED"] as const
export type RegistrationDocumentStatus = (typeof registrationDocumentStatuses)[number]
export type RegistrationRequirement = Readonly<{ id: string; requirementKey: string; label: string; description: string | null; isRequired: boolean; displayOrder: number; status: RegistrationDocumentStatus }>
export type RegistrationCase = Readonly<{ id: string; publicReference: string; garageId: string; leadId: string | null; appointmentId: string | null; customerId: string | null; procedureType: RegistrationProcedureType; procedureTitle: string; status: RegistrationCaseStatus; customerName: string; customerEmail: string | null; customerPhone: string | null; registrationNumber: string | null; brand: string | null; model: string | null; createdAt: string; publicTokenExpiresAt: string | null; requirements: readonly RegistrationRequirement[] }>
export type RegistrationProgress = Readonly<{ requiredCount: number; transmittedCount: number; acceptedCount: number; transmittedPercent: number; acceptedPercent: number; isComplete: boolean }>
export const registrationProcedureLabels: Readonly<Record<RegistrationProcedureType, string>> = { CHANGE_OF_OWNER: "Changement de titulaire", ADDRESS_CHANGE: "Changement d’adresse", DUPLICATE: "Duplicata", REGISTRATION: "Immatriculation", OTHER: "Autre démarche" }
export const registrationStatusLabels: Readonly<Record<RegistrationCaseStatus, string>> = { NEW: "Nouveau", WAITING_FOR_DOCUMENTS: "Documents attendus", DOCUMENTS_RECEIVED: "Documents reçus", UNDER_REVIEW: "À vérifier", INCOMPLETE: "Incomplet", READY: "Prêt", IN_PROGRESS: "En cours", COMPLETED: "Terminé", CANCELLED: "Annulé" }
