import { registrationProcedureLabels, registrationProcedureTypes } from "../types"
import { saveRegistrationProcedure } from "../actions"
type Row = Readonly<{ id: string; procedure_type: string; title: string; description: string | null; is_active: boolean; is_public: boolean; display_order: number; requirements: readonly Readonly<Record<string, unknown>>[] }>
export function RegistrationProcedureSettings({ procedures, canEdit }: { readonly procedures: readonly Row[]; readonly canEdit: boolean }) { return <div className="space-y-5">{registrationProcedureTypes.map((type, index) => { const row = procedures.find((item) => item.procedure_type === type); return <form action={saveRegistrationProcedure} key={type} className="rounded-xl border p-5"><input type="hidden" name="type" value={type}/><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-1 text-sm">Titre<input className="rounded-md border px-3 py-2" name="title" defaultValue={row?.title ?? registrationProcedureLabels[type]} disabled={!canEdit} required/></label><label className="grid gap-1 text-sm">Ordre<input className="rounded-md border px-3 py-2" type="number" name="displayOrder" defaultValue={row?.display_order ?? index} disabled={!canEdit}/></label><label className="grid gap-1 text-sm md:col-span-2">Description<textarea className="rounded-md border px-3 py-2" name="description" defaultValue={row?.description ?? ""} disabled={!canEdit}/></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" name="isActive" defaultChecked={row?.is_active} disabled={!canEdit}/>Démarche activée</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" name="isPublic" defaultChecked={row?.is_public} disabled={!canEdit}/>Visible publiquement</label></div><div className="mt-5 border-t pt-4"><h3 className="font-medium">Ajouter ou mettre à jour une pièce demandée</h3><div className="mt-3 grid gap-3 md:grid-cols-3"><input className="rounded-md border px-3 py-2" name="requirementKey" placeholder="Clé (ex. IDENTITY)" disabled={!canEdit}/><input className="rounded-md border px-3 py-2" name="requirementLabel" placeholder="Libellé client" disabled={!canEdit}/><label className="flex items-center gap-2 text-sm"><input type="checkbox" name="requirementRequired" defaultChecked disabled={!canEdit}/>Obligatoire</label></div>{row?.requirements.length ? <ul className="mt-3 text-sm text-muted-foreground">{row.requirements.map((item) => <li key={String(item.id)}>• {String(item.label)}{item.is_required ? " — obligatoire" : " — facultatif"}</li>)}</ul> : <p className="mt-3 text-sm text-muted-foreground">Aucune pièce configurée.</p>}</div>{canEdit ? <button className="mt-5 rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">Enregistrer la démarche</button> : null}</form> })}<p className="text-xs text-muted-foreground">Les pièces demandées peuvent varier selon la situation. Le garage reste responsable de sa configuration.</p></div> }
