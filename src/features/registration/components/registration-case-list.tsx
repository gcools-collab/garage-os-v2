import Link from "next/link"
import type { RegistrationCaseViewModel } from "../builders"
export function RegistrationCaseList({ cases }: { readonly cases: readonly RegistrationCaseViewModel[] }) { if (!cases.length) return <div className="rounded-xl border border-dashed p-10 text-center"><h2 className="font-semibold">Aucun dossier carte grise</h2><p className="mt-2 text-sm text-muted-foreground">Les dossiers apparaîtront ici après une demande configurée.</p></div>; return <div className="overflow-hidden rounded-xl border"><div className="divide-y">{cases.map((item) => <article key={item.id} className="grid gap-3 p-4 md:grid-cols-[1fr_1fr_auto] md:items-center"><div><Link href={`/registration/${item.id}`} className="font-semibold underline-offset-4 hover:underline">{item.reference}</Link><p className="text-sm text-muted-foreground">{item.title} · {item.customer}</p></div><div className="text-sm"><p>{item.vehicle}</p><p className="text-muted-foreground">{item.transmittedLabel} · {item.verifiedLabel}</p></div><span className="w-fit rounded-full border px-3 py-1 text-xs font-medium">{item.status}</span></article>)}</div></div> }
