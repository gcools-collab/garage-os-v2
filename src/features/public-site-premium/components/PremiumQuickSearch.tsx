import type { PremiumHomepageViewModel } from "../presentation"

export function PremiumQuickSearch({ search }: { readonly search: PremiumHomepageViewModel["search"] }) {
  return <form action={search.action} aria-label="Recherche rapide de véhicules" className="grid gap-4 rounded-3xl border border-[var(--live-border)] bg-[var(--live-surface-elevated)] p-5 shadow-[0_20px_60px_var(--live-shadow-color)] md:grid-cols-2 lg:grid-cols-4">{search.fields.map((field) => <label key={field.name} className="grid gap-1.5 text-xs font-medium text-[var(--live-muted-foreground)]">{field.label}{field.type === "select" ? <select name={field.name} className="min-h-11 rounded-xl border border-[var(--live-border)] bg-[var(--live-surface)] px-3 text-sm text-[var(--live-foreground)] focus-visible:outline-2 focus-visible:outline-[var(--live-focus-ring)]"><option value="">{field.placeholder}</option>{field.options.map((option) => <option key={option} value={option}>{option}</option>)}</select> : <input type="number" min="0" name={field.name} placeholder={field.placeholder} className="min-h-11 rounded-xl border border-[var(--live-border)] bg-[var(--live-surface)] px-3 text-sm text-[var(--live-foreground)] focus-visible:outline-2 focus-visible:outline-[var(--live-focus-ring)]" />}</label>)}<button className="min-h-12 rounded-xl bg-[var(--live-primary)] px-6 font-semibold text-[var(--live-primary-foreground)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--live-focus-ring)] lg:col-span-4">{search.submitLabel}</button></form>
}
