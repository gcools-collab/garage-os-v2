import { CheckCircle2, CircleAlert, Sparkles, XCircle } from "lucide-react"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import type { MediaQualityViewModel } from "../presentation"

export function MediaQualityReportCard({ report }: { readonly report: MediaQualityViewModel }) {
  return <Card><CardHeader><div className="flex flex-wrap items-start justify-between gap-4"><div><CardTitle>Qualité des médias</CardTitle><CardDescription>{report.summary}</CardDescription></div><div className="text-right"><p className="text-3xl font-semibold tabular-nums">{report.scoreLabel}</p><p className="text-xs text-muted-foreground" aria-label={report.starsLabel}>{report.stateLabel}</p></div></div></CardHeader><CardContent className="grid gap-6 lg:grid-cols-2"><section><h3 className="font-medium">Guide de prise de vue</h3><ul className="mt-3 grid gap-2 sm:grid-cols-2">{report.angles.map((angle) => <li key={angle.id} className="flex items-center gap-2 text-sm">{angle.covered ? <CheckCircle2 className="size-4 text-emerald-600" aria-hidden /> : <XCircle className="size-4 text-destructive" aria-hidden />}<span>{angle.label} {angle.covered ? "validé" : "manquant"}</span></li>)}</ul></section><section><h3 className="font-medium">Points à vérifier</h3>{report.weaknesses.length ? <ul className="mt-3 space-y-2">{report.weaknesses.map((item) => <li key={item} className="flex gap-2 text-sm"><CircleAlert className="mt-0.5 size-4 shrink-0 text-amber-600" aria-hidden />{item}</li>)}</ul> : <p className="mt-3 text-sm text-muted-foreground">Aucun défaut déterministe détecté.</p>}<h3 className="mt-5 flex items-center gap-2 font-medium"><Sparkles className="size-4" aria-hidden />{report.aiLabel}</h3>{report.suggestions.length ? <ul className="mt-3 space-y-2 text-sm">{report.suggestions.map((item) => <li key={item}>{item}</li>)}</ul> : <p className="mt-3 text-sm text-muted-foreground">Les validations techniques restent disponibles sans IA.</p>}</section></CardContent></Card>
}
